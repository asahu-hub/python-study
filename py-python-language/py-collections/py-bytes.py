print('When bytes are decoded they become String and when Strings are encoded they become bytes')
print('Bytes are immuatable sequences of bytes')

a=b'Example bytes with single quotes'
b=b"Example bytes with double quotes"

print(a)
print(b)